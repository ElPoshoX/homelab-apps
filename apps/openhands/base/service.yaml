apiVersion: v1
kind: Service
metadata:
  name: openhands
  namespace: openhands
spec:
  selector:
    app: openhands
  ports:
    - name: http
      port: 3000
      targetPort: http
      protocol: TCP
