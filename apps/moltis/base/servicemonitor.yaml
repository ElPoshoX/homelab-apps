apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: moltis
  labels:
    app.kubernetes.io/name: moltis
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: moltis
  endpoints:
    - port: gateway
      path: /metrics
      interval: 15s
