apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

labels:
  - includeSelectors: true
    pairs:
      app.kubernetes.io/managed-by: kustomize
      app.kubernetes.io/part-of: aegis
      homelab.local/OwnedBy: ElPoshoX

namespace: aegis-postgres

resources:
  - namespace.yaml
  - secret.yaml
  - cluster.yaml
  - ingress.yaml
  - network-policy.yaml
