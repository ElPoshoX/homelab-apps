apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: homarr
  namespace: sambda
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`home.elposhox.dev`)
    services:
    - kind: Service
      name: homarr
      port: 7575

