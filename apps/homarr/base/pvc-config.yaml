apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: homarr
  name: homarr-pvc-config
  namespace: homarr
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi