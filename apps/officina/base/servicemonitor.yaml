apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: officina
  labels:
    app.kubernetes.io/name: officina
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: officina
  endpoints:
    - port: health
      path: /metrics
      interval: 30s
