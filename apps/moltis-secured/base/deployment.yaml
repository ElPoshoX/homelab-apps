apiVersion: apps/v1
kind: Deployment
metadata:
  name: meltis
  labels:
    app: meltis
    app.kubernetes.io/name: meltis
spec:
  replicas: 1
  revisionHistoryLimit: 3
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: meltis
  template:
    metadata:
      labels:
        app: meltis
        app.kubernetes.io/name: meltis
    spec:
      runtimeClassName: kata
      automountServiceAccountToken: false
      initContainers:
        - name: fix-permissions
          image: busybox
          env:
            - name: LITELLM_API_KEY
              valueFrom:
                secretKeyRef:
                  name: meltis-bootstrap
                  key: LITELLM_API_KEY
          command:
            - sh
            - -c
            - |
              set -e
              cp /tmp/moltis-config/moltis.toml /home/moltis/.config/moltis/moltis.toml
              cat > /home/moltis/.config/moltis/provider_keys.json <<KEYS
              {"custom-litellm":{"apiKey":"$LITELLM_API_KEY"}}
              KEYS
              mkdir -p /home/moltis/.moltis/agents /home/moltis/.moltis/hooks /home/moltis/.moltis/logs /home/moltis/.moltis/cron /home/moltis/.moltis/skills
              cp /tmp/moltis-agents/*.md /home/moltis/.moltis/agents/
              cp /tmp/moltis-hooks/*.sh /home/moltis/.moltis/hooks/
              cp /tmp/moltis-cron/jobs.json /home/moltis/.moltis/cron/jobs.json
              mkdir -p /home/moltis/.moltis/skills/vault-write
              cp /tmp/moltis-skills/vault-write.md /home/moltis/.moltis/skills/vault-write/SKILL.md
              chmod +x /home/moltis/.moltis/hooks/*.sh
              chown -R 1000:1000 /home/moltis/.config/moltis /home/moltis/.moltis
          securityContext:
            runAsUser: 0
          volumeMounts:
            - name: meltis-config
              mountPath: /home/moltis/.config/moltis
            - name: meltis-data
              mountPath: /home/moltis/.moltis
            - name: meltis-toml
              mountPath: /tmp/moltis-config
            - name: meltis-agents
              mountPath: /tmp/moltis-agents
            - name: meltis-hooks
              mountPath: /tmp/moltis-hooks
            - name: meltis-cron
              mountPath: /tmp/moltis-cron
            - name: vault-write-skill
              mountPath: /tmp/moltis-skills

      containers:
        - name: meltis
          image: ghcr.io/moltis-org/moltis:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 13131
              name: gateway
            - containerPort: 13132
              name: http-ca
            - containerPort: 1455
              name: oauth
          env:
            - name: MOLTIS_NO_TLS
              value: "true"
            - name: MOLTIS_BEHIND_PROXY
              value: "true"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://opentelemetry-collector.otel-collector.svc.cluster.local:4317"
            - name: OTEL_SERVICE_NAME
              value: "moltis"
            - name: MOLTIS_VAULT_AUTO_UNSEAL_KEY_FILE
              value: /run/secrets/recovery-key
            - name: DOCKER_HOST
              value: unix:///run/docker/docker.sock
          envFrom:
            - secretRef:
                name: meltis-bootstrap
          volumeMounts:
            - name: meltis-config
              mountPath: /home/moltis/.config/moltis
            - name: meltis-data
              mountPath: /home/moltis/.moltis
            - name: meltis-toml
              mountPath: /tmp/moltis-config
            - name: vault-recovery
              mountPath: /run/secrets
              readOnly: true
            - name: docker-socket
              mountPath: /run/docker
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 2Gi
          startupProbe:
            httpGet:
              path: /
              port: gateway
            initialDelaySeconds: 5
            periodSeconds: 10
            failureThreshold: 30
          livenessProbe:
            httpGet:
              path: /
              port: gateway
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /
              port: gateway
            initialDelaySeconds: 10
            periodSeconds: 10
            failureThreshold: 3
          securityContext:
            runAsUser: 1000
            runAsGroup: 1000
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL

        - name: dind
          image: docker:27-dind
          args:
            - dockerd
            - --host=unix:///run/docker/docker.sock
            - --iptables=false
            - --ip6tables=false
            - --bridge=none
          securityContext:
            privileged: true
          env:
            - name: DOCKER_TLS_CERTDIR
              value: ""
          volumeMounts:
            - name: dind-storage
              mountPath: /var/lib/docker
            - name: docker-socket
              mountPath: /run/docker
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: "2"
              memory: 4Gi

      securityContext:
        fsGroup: 1000
      volumes:
        - name: meltis-config
          persistentVolumeClaim:
            claimName: meltis-config
        - name: meltis-data
          persistentVolumeClaim:
            claimName: meltis-data
        - name: meltis-toml
          configMap:
            name: meltis-config
        - name: vault-recovery
          secret:
            secretName: meltis-vault-recovery-key
            items:
              - key: recovery-key
                path: recovery-key
        - name: docker-socket
          emptyDir: {}
        - name: dind-storage
          emptyDir:
            sizeLimit: 10Gi
        - name: meltis-agents
          configMap:
            name: meltis-agents-config
        - name: meltis-cron
          configMap:
            name: meltis-cron-jobs
        - name: meltis-hooks
          configMap:
            name: meltis-hooks
            defaultMode: 0755
        - name: vault-write-skill
          configMap:
            name: meltis-vault-write-skill
