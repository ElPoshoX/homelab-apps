apiVersion: isindir.github.com/v1alpha3
kind: SopsSecret
metadata:
  name: tempo-s3-credentials
  namespace: tempo
spec:
  suspend: false
  secretTemplates:
    - name: tempo-s3-credentials
      stringData:
        access_key: garage
        secret_key: garage
