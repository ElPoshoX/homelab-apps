replicaCount: 1

serviceMonitor:
  enabled: true
  namespace: pushgateway

resources:
  requests:
    cpu: 50m
    memory: 32Mi
  limits:
    cpu: 100m
    memory: 64Mi

persistentVolume:
  enabled: false

service:
  type: ClusterIP
  port: 9091
